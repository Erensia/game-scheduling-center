# 01. 요구사항 정의서 (기획안)

> 이 문서는 지금의 정적 프론트엔드(파밍 관제소)가 로컬에 저장하며 제공하고 있는 기능을,
> Spring Boot 백엔드가 대체/지원해야 할 요구사항으로 정리한 것이다.
> 새 기능을 추가하는 문서가 아니라, **현재 동작을 스펙으로 고정**하는 것이 1차 목표다.

## 1. 배경 및 목적

- 현재 버전은 게임 캐릭터 육성/주간 컨텐츠 관리 데이터를 브라우저의 `localStorage`에만 저장한다.
- 기기를 바꾸거나 브라우저 데이터가 삭제되면 데이터가 유실되며, 여러 기기 간 동기화가 불가능하다.
- Spring Boot 기반 REST API 백엔드를 구축해 데이터를 서버(DB)에 영속화하고,
  프론트엔드는 UI/렌더링 로직을 그대로 유지한 채 데이터 계층만 API 호출로 교체한다.

## 2. 범위 (MVP 기준)

### 포함 (MVP)
- 게임(Game) 등록/조회/수정/삭제
- 캐릭터(Character) 등록/조회/수정(완료 토글 포함)/삭제
- 캐릭터별 육성 체크리스트 항목(Checklist Item) 추가/토글/삭제
- 주간 컨텐츠(Weekly Content) 등록/조회/완료 토글/삭제, 게임별 리셋 요일 설정
- 캐릭터 육성 템플릿(Template) 저장/조회/삭제, 캐릭터 생성 시 템플릿 적용
- 파티(Party) 구성 — 파티 생성/이름 변경/슬롯에 캐릭터 배정/삭제 (생성 시점의 게임 `partySize`를 파티가 스냅샷으로 저장, 그 값만큼 슬롯 자동 생성)
- 기존 데이터(localStorage 백업 JSON) 초기 이관 — API가 아닌 **1회성 스크립트**로 처리 (자세한 내용은 7절, `05-migration-plan.md` 참고)

### 제외 (MVP 이후로 미룸)
- 로그인/회원가입, 사용자별 데이터 분리 (1인 개인용으로 우선 개발 — 인증은 2단계 목표)
- 실시간 동기화(웹소켓 등)
- 여러 사용자 간 데이터 공유/열람
- 지속적인 백업/복원 API (사용자가 이후에도 계속 쓰는 내보내기/가져오기 기능 — 7절 참고)

## 3. 기능 요구사항 (현재 프론트엔드 기준)

| ID | 기능 | 설명 |
|---|---|---|
| FR-1 | 게임 CRUD | 이름, 주간 리셋 요일(0~6), 파티 인원수(`partySize`)를 가진 게임을 생성/조회/수정/삭제 |
| FR-2 | 캐릭터 CRUD | 게임에 속한 캐릭터 생성/조회/삭제, 완료 여부(`completed`) 토글 |
| FR-3 | 체크리스트 항목 CRUD | 캐릭터에 속한 자유 텍스트 항목 추가/완료 토글/삭제 |
| FR-4 | 주간 컨텐츠 CRUD | 게임에 속한 주간 컨텐츠 항목 추가/삭제, 완료 토글 |
| FR-5 | 주간 리셋 로직 | 게임별 리셋 요일 기준으로 "이번 주" 완료 여부를 계산 (리셋 요일이 지나면 자동으로 미완료로 표시) |
| FR-6 | 템플릿 CRUD | 캐릭터의 체크리스트 구성을 이름 붙여 게임 단위로 저장, 목록 조회, 삭제 |
| FR-7 | 템플릿 적용 | 캐릭터 생성 시 템플릿을 선택하면 체크리스트 항목이 그대로 복사되어 생성 |
| FR-8 | 파티 CRUD | 게임에 속한 파티 생성/이름 변경/삭제. 파티는 생성 시점의 게임 `partySize`를 자신의 값으로 스냅샷 저장하고, 그만큼 슬롯이 자동 생성되며 슬롯별 캐릭터 배정(빈 슬롯 허용) |
| FR-9 | 홈 대시보드 집계 | 전체 게임을 가로질러 미완료 주간 컨텐츠 목록, 육성 필요 캐릭터 목록을 게임별로 그룹핑해 반환 |

> 초기 데이터 이관은 API 기능이 아니라 1회성 스크립트로 처리하므로 위 표에서 제외했다 (`05-migration-plan.md` 참고).

## 4. 비기능 요구사항 (초안 — 06-tech-stack.md에서 구체화)

- 개인용 포트폴리오/실사용 목적으로, 대규모 트래픽을 고려하지 않는다.
- 응답 지연보다 코드 구조(계층 분리, 테스트 가능성)의 명확성을 우선한다.
- 프론트엔드는 그대로 유지하고, API 계약(명세서)이 바뀌면 프론트엔드 수정 범위를 최소화할 수 있도록 설계한다.

## 5. 용어 기준

용어 정의는 `02-domain-glossary.md` 참고.

## 6. 미해결 사항 (TBD)

- [x] ~~MVP 이후 인증 도입 시점과 방식~~ → **결정: MVP 범위에서 완전히 제외**
- [x] ~~파티 슬롯 개수를 고정(4개)할지, 게임/컨텐츠별로 가변으로 둘지~~ → **결정: 게임 단위로 가변**
- [x] ~~백업 JSON 가져오기를 API로 제공할지, 최초 1회 수동 스크립트로 처리할지~~ → **결정: 초기 이관은 스크립트, 지속 백업/복원 기능은 MVP 제외**
- [ ] 동시 편집/충돌 처리 정책 — **배포 방식 확정 후 재논의 (보류)**
- [x] ~~소프트 삭제 vs 하드 삭제~~ → **결정: 전 엔티티 하드 삭제로 통일** (현재 프론트엔드와 동일한 방식 유지)

### 논의 중: 동시 편집/충돌 처리 정책

- **보류 이유**: 이 문제의 발생 여부 자체가 배포 방식에 달려있다. 로컬 PC 한 대에서만 사용한다면 동시 편집 상황이 사실상 발생하지 않지만, GitHub Pages + 클라우드 DB로 배포해 여러 기기(폰/PC)에서 접속하게 되면 실제로 발생할 수 있다. 배포 방식이 아직 미정이므로, 지금 낙관적 락(`version` 컬럼) 등을 설계하는 것은 시기상조로 판단해 보류한다.
- **다음 단계**: 배포/운영 문서(`07-deployment.md`, 현재 자리만 만들어둠) 작성 시점에 배포 방식이 정해지면 이 항목을 재논의한다.

## 7. 결정 로그 (상세 근거)

### 인증은 MVP에서 제외

- **결정**: MVP는 인증 없이, 1인 개인용 백엔드로 개발한다. 여러 사용자 지원이 필요해지는 시점에 별도 단계로 추가한다.
- **근거**:
  1. 이 프로젝트의 목적은 도메인 모델링 + REST API 설계 역량을 보여주는 것이며, 인증 자체는 목적이 아니다.
  2. Todo List 프로젝트에서 이미 BCrypt 기반 인증/보안을 다뤘으므로, 포트폴리오 세트 안에서 역할을 중복시키지 않는다 (이 프로젝트는 도메인 모델링, Todo List는 인증/보안).
  3. 개인용 도구 성격상 무인증 구조가 이 단계에 맞는 합리적인 설계다.
- **후속 조치**: 나중에 인증을 추가할 때 스키마 리팩토링 충격을 줄이기 위해, ERD에는 "인증 도입 시 `Game`에 `ownerId`(FK to User)가 추가될 것"이라는 확장 메모만 남겨둔다 (`03-erd.md` 참고). 지금 컬럼을 미리 만들지는 않는다.

### 파티 슬롯 개수는 게임 단위로 가변

- **결정**: 파티(Party)마다가 아니라 **게임(Game)에 `partySize` 필드를 두고**, 그 게임에 속한 모든 파티는 이 값만큼의 슬롯을 갖는다. 전역 하드코딩(4개)은 쓰지 않는다.
- **근거**:
  1. 같은 게임 안에서는 모든 컨텐츠가 동일한 인원수를 요구하므로(예: 명조·젠존제는 3인 파티), 슬롯 개수는 파티별이 아니라 게임별 속성이 맞다.
  2. 향후 다른 인원수를 요구하는 게임(예: 4인 파티 게임)이 추가돼도 스키마·API 구조 변경 없이 게임 데이터만 다르게 넣으면 된다.
  3. 작업량 대비 이점이 커서(ERD 필드 1개, API 로직 소폭 추가) 지금 반영하는 비용이 낮다.
- **부가 발견**: 현재 프론트엔드는 파티 생성 시 슬롯 개수가 `[null,null,null,null]`로 4개 하드코딩되어 있는데, 실제 명조·젠존제는 3인 파티다. 즉 이 결정은 미래 대비뿐 아니라 **지금 존재하는 어긋난 하드코딩을 바로잡는 작업이기도 하다.** (9절 참고)

### 초기 데이터 이관은 스크립트로, 지속적인 백업/복원 기능은 MVP에서 제외

기존에 하나의 TBD로 묶여있었지만, 실제로는 성격이 다른 두 가지 결정이라 분리한다.

**① 초기 데이터 이관 (일회성)**
- **결정**: 기존 `localStorage`/백업 JSON에 쌓인 데이터를 백엔드로 옮기는 작업은, 영구 API 엔드포인트가 아니라 **1회성 스크립트**로 처리한다.
- **근거**: 백엔드를 처음 구축하는 시점에 단 한 번 실행하고 끝나는 작업이라, 이를 위해 지속적으로 유지·검증해야 하는 API 엔드포인트를 만드는 것은 과한 설계다. 데이터 소스도 이미 정해진 백업 파일 하나뿐이라 스크립트로도 충분히 안정적이다.
- **`04-api-spec.md`의 `/migration/import` 엔드포인트는 제거**하고, 이 결정을 반영해 스크립트 실행 절차로 대체한다 (`05-migration-plan.md` 참고).

**② 지속적인 백업/복원 기능 (사용자가 이후에도 계속 쓰는 기능)**
- **결정**: MVP 범위에서 제외한다. 인증과 동일한 판단 기준을 적용.
- **근거**: 없어도 핵심 기능(게임/캐릭터/체크리스트/주간컨텐츠 관리)은 완결된다. 서버가 데이터를 영속화하는 이상, 사용자가 수동으로 매번 백업할 필요성 자체가 지금(localStorage 시절)보다 낮아진다. 다만 DB 초기화·호스팅 이전 등에 대비한 백업 필요성은 여전히 있으므로, 필요해지면 별도 기능으로 설계한다.
- **서버 부하는 이 결정의 근거가 아니다** — 개인용 규모에서 백업/복원처럼 가끔 호출되는 API는 부하 요인이 되지 않는다. 판단 기준은 어디까지나 "MVP 핵심 범위에 필요한가"였다.

### 삭제 방식은 하드 삭제로 통일

- **결정**: 소프트 삭제를 도입하지 않고, 모든 엔티티에 지금의 프론트엔드와 동일하게 **하드 삭제**를 적용한다.
- **근거**: 컬럼(`deletedAt`)과 조회 시 필터링 로직을 추가하는 비용, 그리고 소프트 삭제된 캐릭터를 사용자가 인지하지 못해 동일한 정보를 중복으로 재입력하게 될 위험까지 감안하면, 지금 규모(개인용, 데이터량 적음)에서는 하드 삭제가 더 단순하고 실용적이라고 판단했다.
- **별개 사항**: "캐릭터 육성 스펙 비교" 같은 향후 확장 기능은 이 결정과 무관하다 — 애초에 소프트 삭제로 해결되는 문제가 아니라 별도의 이력/스냅샷 테이블이 필요한 사안이므로, 8절 백로그로 분리해 관리한다.

### PK 타입, 감사(auditing) 컬럼, PartySlot 저장 방식, 대시보드 페이지네이션

- **PK 타입**: `Long`(auto increment)으로 확정. 기존 Spring Boot 포트폴리오 프로젝트(GCAS, Todo List)에서 이미 일관되게 써온 방식과 통일한다.
- **감사 컬럼**: `createdAt`은 전 엔티티에 일괄 적용(Spring Data JPA Auditing으로 구현 부담이 적음). `updatedAt`은 현재 어떤 기능도 "마지막 수정 시각"을 요구하지 않으므로 MVP에서는 도입하지 않는다.
- **PartySlot 저장 방식**: 정규화된 별도 테이블로 확정. JSON 컬럼과 비교해 슬롯 단위 PATCH API를 단일 행 UPDATE로 단순하게 구현할 수 있고, 캐릭터 삭제 시 슬롯을 null 처리하는 무결성도 DB 레벨에서 관리하기 쉽다. (DB 저장 방식과 API 응답 포맷은 별개의 문제이며, 어느 쪽을 택해도 클라이언트가 받는 JSON 응답은 동일하다는 점에 유의.)
- **대시보드 페이지네이션**: 서버에서 처리하는 것으로 확정. 데이터가 늘어나는 상황을 감안해, `/dashboard` 응답을 클라이언트가 전체를 받아 자르는 대신 서버가 페이지 단위로 끊어 내려준다.

### 게임의 `partySize` 변경이 기존 파티에 영향을 주지 않도록 스냅샷 방식 채택

TBD였던 "`partySize`를 나중에 바꾸면 기존 파티 슬롯을 어떻게 처리할지" 문제를, 회피가 아니라 **문제 자체가 발생하지 않는 구조**로 해결한다.

- **결정**: `Party`는 생성 시점의 `Game.partySize` 값을 자신의 `partySize` 필드에 복사해서 저장한다 (스냅샷). 이후 `PartySlot` 생성·검증은 게임이 아니라 **파티 자신의 `partySize`**를 기준으로 한다.
- **효과**: `Game.partySize`를 나중에 바꿔도 이미 만들어진 파티는 전혀 영향받지 않는다 (자기 값을 그대로 유지). 새로 만드는 파티부터만 바뀐 값을 따라간다.
- **근거**: 흔히 쓰는 "스냅샷" 패턴(예: 주문 시점의 가격을 주문 항목에 복사해두는 것)과 같은 원리다. 정수 컬럼 하나를 추가하는 정도라 비용이 거의 없고, `Game.partySize`를 아예 수정 불가로 막는 것보다 유연하다 — 게임 설정은 계속 바뀔 수 있게 열어두면서, 이미 구성해둔 파티는 안전하게 보호된다.
- **파급 효과**: `04-api-spec.md`의 파티 생성 API 설명, `INVALID_SLOT_INDEX` 에러 기준을 "게임의 `partySize`"에서 "파티 자신의 `partySize`"로 정정. `03-erd.md`의 `PARTY` 엔티티에 `partySize` 필드 추가.

### 파티 인원수(partySize) 가변화가 마이그레이션에 미치는 영향

- 기존 `localStorage` 데이터에는 `partySize` 개념 자체가 없었고, 구 프론트엔드가 파티 슬롯을 4개로 고정해왔기 때문에 기존 데이터만으로는 게임별 정확한 인원수(예: 명조·젠존제 = 3명)를 자동으로 판별할 수 없다.
- **결정**: 이관 스크립트 실행 전, 게임별 정확한 `partySize` 값을 사람이 직접 입력하는 설정 단계를 추가한다. 이관되는 각 파티는 이 값을 자신의 `partySize` 스냅샷으로 저장한다 (`05-migration-plan.md` 참고).

### 체크리스트/템플릿 항목 정렬 방식

- **결정**: 재정렬(드래그로 순서 변경) API는 지원하지 않는다. 현재 프론트엔드에도 없는 기능이고 필요성도 낮다고 판단.
- **`sortOrder` 컬럼은 만들지 않고, 자동 증가 `id` 오름차순으로 정렬한다.** `createdAt` 타임스탬프보다 `id`가 동시 요청 상황에서도 항상 유일한 순서를 보장해 더 안전하다.
- 조회 시 서비스 레이어에서 `ORDER BY id ASC`를 명시해 JPA의 컬렉션 순서 미보장 문제를 해결한다 (`03-erd.md`, `04-api-spec.md` 참고).

## 8. 백로그 (범위 밖이지만 향후 검토)

| 항목 | 메모 |
|---|---|
| 캐릭터 육성 이력 스냅샷 | "육성 스펙 비교" 기능을 위해서는 소프트 삭제와 별개로 시점별 상태를 기록하는 이력/스냅샷 테이블 설계가 필요. 별도 기능으로 검토 |

## 9. 프론트엔드 반영 필요 사항 (백엔드 연동과 별개로 처리)

| 항목 | 내용 |
|---|---|
| 파티 인원수 하드코딩 수정 | 현재 `app.js`의 파티 생성 로직이 슬롯 4개로 고정되어 있음. 게임별 `partySize`를 설정할 수 있는 UI(게임 추가/수정 시 입력)가 필요하며, 파티 생성 시 그 값만큼 슬롯을 만들도록 수정해야 함 |
