# 파밍 관제소 — 개발 문서

## 1. 프로젝트 배경

모바일 가챠 게임(명조, 젠존제 등)을 플레이하면서 겪는 공통적인 문제에서 출발했다.

> 버전이 업데이트될수록 보유 캐릭터와 해야 할 컨텐츠가 계속 늘어나고, 그에 따라 "지금 뭘 해야 하는지"에 대한 플랜을 점점 잃어버리게 된다.

이 문제를 해결하기 위해, 게임별로 캐릭터 육성 상태와 주간 컨텐츠 진행 상황을 한눈에 파악할 수 있는 개인용 웹앱을 제작하기로 했다.

## 2. 초기 요구사항

기획 단계에서 정리된 핵심 요구사항은 다음과 같다.

| 구분 | 요구사항 |
|---|---|
| 캐릭터 육성 관리 | 게임 카테고리별로 캐릭터의 현재 육성 상태를 확인할 수 있어야 한다 |
| | 장비(에코, 디스크 등)를 더 이상 파밍하지 않아도 되는 상태인지 확인할 수 있어야 한다 |
| 주간 컨텐츠 관리 | 어떤 주간 컨텐츠를 완료했고, 어떤 것을 더 해야 하는지 확인할 수 있어야 한다 |
| 파티 구성 (우선순위 낮음) | 다수 파티가 필요한 컨텐츠에 대비해 보유 캐릭터로 파티를 미리 구성해볼 수 있으면 좋겠다 |

추가로, 개발 과정에서 다음 두 가지 세부 방향이 확정되었다.

- **육성 체크 항목은 게임마다 다르므로, 정해진 항목이 아닌 자유 텍스트로 커스텀할 수 있도록 한다.** (AI가 미리 채워주는 방식이 아니라 사용자가 직접 항목을 정의)
- **캐릭터 목록도 AI가 미리 채우지 않는다.** 게임 이름의 AI 번역이 불안정할 수 있어, 처음부터 빈 상태에서 사용자가 직접 캐릭터를 추가하는 방식으로 결정.

## 3. 디자인 방향

기능 위주의 개인용 관리 도구이지만, "게임 상태를 관제하는 대시보드"라는 컨셉에 맞춰 시각적 정체성을 부여했다.

- **컬러**: 다크 그래파이트 배경(`#12151A`) 위에 앰버(`#FF9F45`, 미완료/파밍 필요 상태)와 틸(`#4FD1C5`, 완료 상태) 두 가지 시그널 컬러를 대비시켜, 상태 정보를 색으로 즉시 구분할 수 있게 했다.
- **타이포그래피**: 헤더/캐릭터 이름에는 대시보드 느낌의 `Barlow Semi Condensed`, 본문에는 `Inter`, 수치·날짜 등 데이터 표기에는 `JetBrains Mono`를 사용해 "관제실 UI" 톤을 유지했다.
- **시그니처 요소**: 육성 완료된 캐릭터 카드에는 도장처럼 살짝 기울어진 "완료" 스탬프를 표시해, 체크리스트를 다 채웠을 때의 성취감을 시각적으로 표현했다.
- **레이아웃**: 좌측 사이드바에서 게임(명조, 젠존제 등)을 전환하고, 우측 메인 영역에서 캐릭터 육성 / 주간 컨텐츠 / 파티 구성 탭을 오가는 대시보드형 구조로 설계했다.

## 4. 최종 구현 기능

### 4.1 게임 관리
- 좌측 사이드바에서 게임을 자유롭게 추가/삭제
- 게임별로 캐릭터, 주간 컨텐츠, 파티 데이터가 독립적으로 관리됨

### 4.2 캐릭터 육성 탭
- 캐릭터 이름을 직접 입력해 추가
- 캐릭터별로 육성 체크 항목을 자유 텍스트로 추가 (예: 무기 레벨, 에코 세팅)
- 항목 체크 시 취소선 처리
- "더 이상 파밍 안 해도 됨" 토글로 캐릭터를 완료 상태로 표시
- 전체 / 진행중 / 완료 필터로 캐릭터 목록을 걸러서 확인

### 4.3 주간 컨텐츠 탭
- 게임별 주간 리셋 요일 설정 (다음 리셋까지 남은 일수 표시)
- 주간 컨텐츠 항목 추가 및 완료 체크
- 설정한 리셋 요일이 지나면 완료 상태가 자동으로 초기화됨

### 4.4 파티 구성 탭 (우선순위 낮은 기능으로 시작 → 구현 완료)
- 여러 개의 파티를 생성
- 각 파티 슬롯에 드롭다운으로 보유 캐릭터를 배정
- 파티 이름 변경 및 삭제 가능

### 4.5 요약 스트립
- 선택한 게임의 전체 캐릭터 수, 육성 필요 캐릭터 수, 이번 주 남은 컨텐츠 수를 상단에 항상 표시

### 4.6 홈 대시보드 — 오늘 할 일 통합뷰 (확장 기능)
- 사이드바 상단의 "🏠 오늘 할 일" 버튼으로 진입하는 전용 화면
- 게임을 하나씩 선택해서 봐야 했던 기존 구조와 별개로, **등록된 모든 게임을 가로질러** 아직 끝내지 않은 일을 한 화면에 모아 보여줌
- 구성:
  - 상단 통계 카드: 등록된 게임 수, 전체 게임에서 육성이 필요한 캐릭터 수, 이번 주 남은 컨텐츠 수
  - "이번 주 남은 컨텐츠" 섹션: 게임별로 그룹지어 미완료 주간 컨텐츠를 나열, 이 화면에서 바로 체크 가능
  - "육성이 필요한 캐릭터" 섹션: 게임별로 그룹지어 미완료 캐릭터를 나열, 이 화면에서 바로 "완료로 표시" 가능
  - 각 그룹에는 "게임으로 이동" 버튼이 있어 해당 게임의 상세 탭으로 바로 이동 가능
- 앱을 열 때 기본 화면으로 이 홈 대시보드가 먼저 보이도록 설정 (게임 관리 도구를 만든 원래 동기인 "전체 진행 상황을 파악하기 어렵다"는 문제에 가장 직접적으로 대응하는 확장)

### 4.7 육성 항목 템플릿 (확장 기능)
- 문제의식: 게임마다 육성 항목의 용어가 다르다 (예: 명조는 "공명체인·에코", 젠존제는 "디스크 드라이브·W-엔진"). 그래서 템플릿을 게임 전체 공용이 아니라 **게임별로 완전히 분리**해서 저장한다.
- 동작 방식:
  1. 캐릭터 카드에 체크리스트 항목을 채운 뒤 "템플릿으로 저장" 버튼을 누르면, 그 항목 구성을 이름 붙여 해당 게임의 템플릿으로 저장
  2. 새 캐릭터를 추가할 때 그 게임에 저장된 템플릿이 있으면 드롭다운에 나타나고, 선택하면 체크리스트가 자동으로 채워짐 (템플릿 없이 추가도 그대로 가능)
  3. 캐릭터 육성 탭 상단에 "저장된 템플릿" 목록이 칩 형태로 표시되며, 개별 삭제 가능
- 항목 용어를 AI가 미리 채워주지 않는다는 원칙은 그대로 유지: 템플릿은 사용자가 직접 만든 체크리스트를 저장하는 것일 뿐, 게임 지식 기반으로 자동 생성되지 않음

## 5. 진행 중 반영된 수정 사항

### 5.1 저장 방식 및 산출물 형태 변경
- **초기 버전**: 단일 HTML 파일 하나로 제작, Claude 아티팩트 환경의 자체 저장소(window.storage)를 사용
- **수정 요청**: "폴더 하나로 구성해주고, 페이지를 닫아도 데이터가 사라지지 않았으면 좋겠다"
- **반영 내용**:
  - `index.html` / `style.css` / `app.js` / `README.md`로 구성된 프로젝트 폴더로 재구성
  - 데이터 저장 방식을 브라우저의 `localStorage`로 전환해, 로컬에서 `index.html`을 직접 열어도 데이터가 영구 보존되도록 변경
  - 만약을 대비해 데이터를 JSON 파일로 내보내기/불러오기 하는 백업 기능 추가

### 5.2 "게임 추가" 버튼 미동작 버그 수정
- **증상**: 사이드바와 메인 화면(빈 상태) 두 곳에 있는 "게임 추가" 버튼 중 한쪽만 정상 동작
- **원인**: 두 버튼의 ID가 다름에도 불구하고, 클릭 이벤트를 `getElementById(A) || getElementById(B)` 방식으로 찾아 둘 중 먼저 발견되는 버튼 하나에만 이벤트를 연결하고 있었음
- **수정**: 두 버튼 각각에 이벤트를 개별적으로 연결하도록 변경

### 5.3 "완료" 스탬프와 삭제 버튼 겹침 수정
- **증상**: 캐릭터 육성 완료 시 카드 우측 상단에 표시되는 "완료" 스탬프가 캐릭터 삭제 버튼과 겹쳐 보임
- **원인**: 삭제 버튼이 `position: absolute`로 카드 우측 상단에 고정되어 있어, 같은 자리에 흐름 배치되는 완료 스탬프와 위치가 충돌
- **수정**: 스탬프와 삭제 버튼을 카드 헤더 안의 같은 그룹으로 묶어, 캐릭터 이름 오른쪽에 "스탬프 → 삭제 버튼" 순서로 나란히 배치. 절대 위치 방식을 제거해 카드 너비나 이름 길이가 달라져도 겹치지 않도록 함

### 5.4 게임을 여러 개 등록했을 때 "게임 추가" 버튼이 화면에서 사라지는 문제 수정
- **증상**: 게임을 하나 추가하고 캐릭터/육성 항목을 채워 콘텐츠가 길어지면, 사이드바가 페이지 전체 길이만큼 함께 늘어나면서 하단의 "게임 추가" 버튼을 보려면 페이지를 한참 아래로 스크롤해야 했음
- **원인**: 사이드바에 별도의 높이 제한이 없어, 메인 콘텐츠 영역이 길어질수록 사이드바도 같이 늘어나는 구조였음
- **수정**: 사이드바를 화면(뷰포트) 높이에 고정(`position: sticky`)하고 내부적으로만 스크롤되도록 변경. 이제 메인 콘텐츠가 아무리 길어져도 "게임 추가" 버튼이 항상 화면 안에 유지됨

## 6. 파일 구조

```
game-scheduling-center/
├── index.html        — 페이지 구조
├── style.css          — 디자인 (컬러 토큰, 레이아웃, 컴포넌트 스타일)
├── app.js             — 상태 관리, 렌더링, 로컬 저장, 백업 기능
├── README.md          — 사용법 안내
├── DEVELOPMENT.md      — 본 개발 문서
└── BK/                — 백업 JSON 파일 보관 폴더
    └── README.md        — 백업 폴더 사용법
```

## 7. 향후 고려 가능한 개선 방향

- 파티 구성 탭에 드래그 앤 드롭 지원
- 캐릭터별 육성 항목을 템플릿으로 저장해 새 캐릭터 추가 시 재사용
- 여러 기기 간 동기화가 필요할 경우, 로컬 저장 방식 외에 별도 백엔드 연동 검토
